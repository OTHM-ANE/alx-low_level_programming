#include "main.h"


/**
 * _isupper - Check if paramter c represents an uppercase letter.
 * @c: The number to be checked
 * Return: 1 for uuper letter or0 for any else
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
